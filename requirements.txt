# Core ML/DL packages
accelerate
datasets
numpy
pandas
transformers<4.48
wandb

# veRL framework dependencies
codetiming
dill
hydra-core
pybind11
ray
tensordict<0.6
vllm<=0.6.3

# Utilities
IPython
matplotlib
requests
tqdm

# Data generation scripts (optional, only needed for generate_sft_data.py)
pyyaml
openai
httpx

# Note: The following packages need special installation order:
# 1. First install torch (with CUDA support):
#    pip install torch==2.4.0 --index-url https://download.pytorch.org/whl/cu121
#    or for CUDA 12.4: pip install torch==2.4.0 torchvision==0.19.0 torchaudio==2.4.0 --index-url https://download.pytorch.org/whl/cu124
# 2. Then install flash-attn:
#    pip install flash-attn --no-build-isolation
# 3. Then install vllm (if not already installed):
#    pip install vllm==0.6.3